/*
 * Common parameters for SIM800 GSM modems
 */

/ {
    fragment@110 {
        target-path = "/wirenboard";

        __overlay__ {
            gsm {
                model = "sim800";
                type = "edge", "gprs";
            };
        };
    };
};
